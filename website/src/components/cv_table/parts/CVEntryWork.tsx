import React from "react";
import {WorkAdditionalInformation} from "../types";

export const CVEntryWork: React.FC<{workInformation: WorkAdditionalInformation}> = ({workInformation}) => {
    return (
        <></>
    )
}